# 🎬 The Movie Archive

Веб-приложение для поиска и фильтрации фильмов с аналитикой пользовательских запросов.

---

## 📋 Описание проекта

The Movie Archive - это веб-приложение на FastAPI, которое предоставляет функционал для поиска фильмов по названию, фильтрации по жанрам и годам, а также собирает аналитику поисковых запросов.

### 🎯 Основные функции
- Поиск фильмов по названию с частичным совпадением
- Фильтрация фильмов по жанрам и диапазонам лет
- Отображение новых фильмов на главной странице
- Сбор и отображение аналитики поисковых запросов
- Пагинация результатов (10 фильмов на страницу)

### 📊 Аналитика
- Сохранение всех поисковых запросов в MongoDB
- Отображение популярных запросов по частоте использования
- Показ истории последних уникальных запросов
- Real-time обновление данных аналитики

---

## 🛠️ Технологический стек

### Backend
- **FastAPI 0.104.1** - веб-фреймворк
- **Uvicorn 0.24.0** - ASGI сервер
- **Pydantic 2.5.0** - валидация данных
- **PyMySQL 1.1.0** - коннектор MySQL
- **PyMongo 4.6.0** - коннектор MongoDB
- **python-dotenv 1.0.0** - управление переменными окружения

### Frontend
- **Jinja2 3.1.2** - шаблонизатор
- **CSS** - стилизация
- **HTML5** - разметка

### Базы данных
- **MySQL** - основная база данных фильмов
- **MongoDB** - хранение аналитики запросов

---

## 📋 Системные требования

- Python 3.11+
- MySQL 5.7+
- MongoDB 4.0+

---

## 🚀 Быстрый старт

### 1️⃣ Клонирование и установка
```bash
git clone https://github.com/ichprojekt/final_project_front.git
cd final_project_front
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

### 2️⃣ Настройка окружения
```bash
cp .env.example .env
```

Отредактируйте `.env` файл:
```env
MYSQL_HOST=127.0.0.1
MYSQL_USER=your_username
MYSQL_PASSWORD=your_password
MYSQL_DB=movies_db
MONGODB_URL_EDIT=mongodb://127.0.0.1:27017/
```

### 3️⃣ Запуск приложения
```bash
uvicorn main:app --reload
```

Приложение будет доступно по адресу: `http://localhost:8000`

---

## 🌐 Эндпоинты API

### Основные страницы
- `GET /` - главная страница с новыми фильмами
- `GET /genre/{genre_name}` - фильмы по конкретному жанру
- `GET /analytics` - страница аналитики
- `GET /analytics/data` - JSON данные аналитики

### Поиск и фильтрация
- `GET/POST /search_title` - поиск по названию фильма
- `GET/POST /search_filter` - фильтрация по жанру и годам

### Статические файлы
- `GET /static/images/posters/{filename}` - постеры фильмов

---

## 📁 Структура проекта

```
final_project_front/
├── main.py                     # Точка входа приложения
├── requirements.txt            # Зависимости Python
├── .env.example               # Шаблон конфигурации
├── README.md                  # Документация
├── app/
│   ├── core/                  # Ядро приложения
│   │   ├── logging.py        # Централизованное логирование
│   │   ├── templates.py      # Настройка Jinja2 шаблонов
│   │   └── exceptions.py     # Обработка ошибок
│   ├── exceptions/            # Обработчики исключений
│   │   └── handlers.py        # Глобальные обработчики ошибок
│   ├── routers/              # Маршруты API
│   │   ├── home.py          # Главная страница
│   │   ├── search.py        # Поиск и фильтрация
│   │   ├── analytics.py     # Аналитика запросов
│   │   └── static.py       # Статические файлы
│   ├── databases/            # Работа с базами данных
│   │   ├── db_mysql.py      # MySQL запросы и подключения
│   │   └── db_mongo.py      # MongoDB аналитика
│   ├── utils/                # Вспомогательные функции
│   │   ├── helpers.py       # Общие данные для шаблонов
│   │   └── validators.py    # Валидация входных данных
│   ├── static/               # Статические ресурсы
│   │   ├── style.css        # Стили приложения
│   │   ├── favicon.ico      # Иконка сайта
│   │   └── images/          # Изображения (постеры)
│   └── templates/            # HTML шаблоны
│       ├── base.html        # Базовый шаблон
│       ├── index.html       # Главная страница
│       ├── results.html     # Результаты поиска
│       └── analytics.html   # Страница аналитики
```

---

## 🏗️ Архитектура приложения

### Двухбазовая архитектура
- **MySQL**: хранение структурированных данных о фильмах, категориях, метаданных
- **MongoDB**: хранение аналитики поисковых запросов в коллекции `final_project_010825-ptm_Serhii_Lanovenkyi`

### Поток обработки запроса
1. HTTP запрос → CORS middleware → Router
2. Валидация входных параметров
3. Поиск данных в MySQL
4. Сохранение поискового запроса в MongoDB
5. Рендеринг HTML шаблона через Jinja2
6. Возврат ответа

### Модульная структура
- **routers/** - обработка HTTP маршрутов
- **databases/** - абстракция работы с базами данных
- **utils/** - переиспользуемые функции (валидаторы, хелперы)
- **core/** - базовые компоненты (логирование, шаблоны, исключения)

---

## 🧾 Логирование

Приложение использует централизованное логирование:
```python
from app.core.logging import get_logger
logger = get_logger(__name__)
```

Уровень логирования по умолчанию: ERROR
Логгер совместим с Uvicorn и выводит ошибки в консоль.

---

## 🛡️ Безопасность

### Валидация данных
- Валидация года выпуска (1900-2100)
- Ограничение длины поисковых запросов (100 символов)
- Валидация номеров страниц (минимум 1)

### Безопасность баз данных
- Параметризованные SQL запросы для защиты от SQL инъекций
- Экранирование пользовательского ввода
- Использование контекстных менеджеров для подключения к БД

### CORS настройки
```python
app.add_middleware(
    CORSMiddleware, 
    allow_origins=["*"],
    allow_credentials=True, 
    allow_methods=["GET", "POST"], 
    allow_headers=["*"]
)
```

---

## 🐛 Решение проблем

### Частые ошибки

#### ModuleNotFoundError
```bash
# Активируйте виртуальное окружение
.venv\Scripts\activate
pip install -r requirements.txt
```

#### Ошибка подключения к MySQL
- Проверьте учетные данные в `.env`
- Убедитесь что MySQL сервер запущен
- Проверьте права доступа пользователя

#### Ошибка подключения к MongoDB
- Убедитесь что MongoDB сервис запущен
- Проверьте URL подключения в `.env`

#### Port already in use
```bash
# Измените порт запуска
uvicorn main:app --port 8001
```

### Диагностика
```bash
# Проверка Python
python --version

# Проверка MySQL
mysql -u username -p -h localhost

# Проверка MongoDB
mongosh mongodb://localhost:27017
```

