# 🎬 The Movie Archive

Современное веб‑приложение для поиска и фильтрации фильмов с аналитикой пользовательских запросов.

---

## ✨ Возможности

### 🎯 Основные функции

* Поиск фильмов по названию
* Фильтрация по жанрам и годам выпуска
* Сбор и отображение аналитики поисковых запросов
* Пагинация результатов (10 фильмов на страницу)
* Адаптивный интерфейс (desktop / mobile)

### 🛡️ Безопасность

* Валидация входных данных
* Использование параметризованных SQL‑запросов
* Экранирование пользовательского ввода
* Ограничение длины поисковых запросов

### 🚀 Производительность

* Оптимизированные SQL‑запросы
* Асинхронная обработка HTTP‑запросов
* Кэширование популярных запросов
* Структурированное логирование

---

## 🛠️ Технологический стек

| Компонент   | Технология                             |
| ----------- | -------------------------------------- |
| Backend     | FastAPI + Uvicorn                      |
| Frontend    | Jinja2 + CSS                           |
| Базы данных | MySQL (основная) + MongoDB (аналитика) |
| Python      | 3.11+ (рекомендуется 3.12)             |

---

## 📋 Требования

* Python **3.11+**
* MySQL **5.7+**
* MongoDB **4.0+**

---

## 🚀 Быстрый старт

### 1️⃣ Клонирование проекта и установка зависимостей

```bash
git clone <repository-url>
cd final_project_front
python -m venv .venv
.venv\Scripts\activate
pip install --upgrade pip
pip install -r requirements.txt
```

---

### 2️⃣ Настройка окружения

Скопируйте файл конфигурации:

```bash
cp .env.example .env
```

Пример `.env`:

```env
MYSQL_HOST=localhost
MYSQL_USER=your_username
MYSQL_PASSWORD=your_password
MYSQL_DB=film_database
MONGODB_URL=mongodb://localhost:27017
```

---

### 3️⃣ Запуск приложения

#### 🧪 Режим разработки

```bash
uvicorn main:app --reload
```

#### 🚀 Продакшен (Linux)

```bash
gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app --bind 0.0.0.0:8000
```

⚠️ **Gunicorn работает только на Linux / macOS**. В Windows используйте `uvicorn`.

Приложение будет доступно по адресу:

```
http://localhost:8000
```

---

## 🌐 Эндпоинты

### Основные страницы

* `GET /` — главная страница
* `GET /genre/{name}` — фильмы по жанру
* `GET /analytics` — страница аналитики
* `GET /analytics/data` — JSON‑данные аналитики

### Поиск и фильтрация

* `POST /search_title` — поиск по названию
* `POST /search_filter` — фильтр по жанру и годам

---

## 📁 Структура проекта

```
final_project_front/
├── main.py
├── requirements.txt
├── .env.example
├── README.md
├── app/
│   ├── core/
│   │   ├── logging.py
│   │   └── templates.py
│   ├── exceptions/
│   ├── routers/
│   │   ├── main.py
│   │   ├── search.py
│   │   └── analytics.py
│   ├── static/
│   │   ├── style.css
│   │   └── favicon.ico
│   ├── templates/
│   │   ├── base.html
│   │   ├── index.html
│   │   ├── results.html
│   │   └── analytics.html
│   └── utils/
│       ├── helpers.py
│       └── validators.py
└── databases/
    ├── db_mysql.py
    └── db_mongo.py
```

---

## 🧾 Логирование

Используется централизованный логгер:

```python
logger = get_logger(__name__)
```

* единый формат логов
* уровни INFO / WARNING / ERROR
* совместимость с Uvicorn

---

## 🐛 Решение проблем

### Проверка MongoDB

```bash
mongod --version
mongosh mongodb://localhost:27017
```

### Проверка MySQL

```bash
mysql --version
mysql -u username -p -h localhost
```

### Частые ошибки

* Убедитесь, что используется **Python 3.11+**
* Проверьте наличие файла `.env`
* Убедитесь, что функция подключения к БД называется:

```python
get_db_connection()
```

---

## 📄 Лицензия

MIT License — свободное использование и модификация.

---

## 👨‍💻 Автор

**Sergii Lanovenkyi**
GitHub: [https://github.com/ichprojekt](https://github.com/ichprojekt)

---

⭐ Если проект был полезен — поставьте звезду репозиторию
